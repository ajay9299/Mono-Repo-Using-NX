{
  "name": "aws-clients",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "aws-clients/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/aws-clients",
        "main": "aws-clients/src/index.ts",
        "tsConfig": "aws-clients/tsconfig.lib.json",
        "assets": ["aws-clients/*.md"]
      }
    }
  }
}
